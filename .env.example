# Required only if you want the /demo form to actually send email.
# Without these, the form still shows a success state to visitors — the submission is silently dropped.
# Set these in Vercel → Project → Settings → Environment Variables.

RESEND_API_KEY=
FOUNDER_EMAIL=
